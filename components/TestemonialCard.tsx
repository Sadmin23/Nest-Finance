'use client';

import Image from 'next/image';
import { Button } from 'antd';

const TestemonialCard = (): JSX.Element => {
  return (
    <div className='w-[465px] h-[294px] bg-white bg-opacity-50 rounded-2xl'>
    </div>
  );
};

export default TestemonialCard;
